# @sherlock/tsconfig — Shared TypeScript Configurations

## Purpose

Centralized TypeScript configuration profiles for the monorepo.

## Profiles

- `base.json` — Strict base configuration
- `node.json` — Backend services (NestJS, scripts)
- `react.json` — Frontend applications (Next.js)
