# @sherlock/eslint-config — Shared ESLint Configuration

## Purpose

Centralized ESLint configuration with TypeScript-aware rules for all packages.

## Usage

```json
{
  "extends": ["@sherlock/eslint-config"]
}
```
